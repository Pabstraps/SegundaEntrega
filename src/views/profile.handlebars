<div>
    <h1>Tu perfil (sin datos sensibles)</h1>
    <p>{{user.name}}</p>
    <p>{{user.email}}</p>
    <p>{{user.age}}</p>


    <form action="users/request-password-reset/" method="POST">
        <input type="hidden" name="email" value="{{user.email}}">
        <button type="submit">Reset Password</button>
    </form>
</div>